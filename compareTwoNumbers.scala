object compareTwoNumbers {
def main(args:Array[String]) :Unit = {
  val a = 80
  val b = 50
  if (a > b)
    { println (s" ${a} is greatest")}
  else
    { println(s"${b} is greatest")}
}
}
